name: Build Mercury EXE

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest

    steps:
    - name: 📥 Checkout code
      uses: actions/checkout@v3

    - name: ⚙️ Setup Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '18'

    - name: 📦 Install dependencies
      run: npm install

    - name: 🛠️ Package Electron app for Windows
      run: npx electron-packager ./public Mercury --platform=win32 --arch=x64 --out=dist --overwrite

    - name: 📤 Upload built app
      uses: actions/upload-artifact@v4
      with:
        name: mercury-windows-exe
        path: dist/Mercury-win32-x64
